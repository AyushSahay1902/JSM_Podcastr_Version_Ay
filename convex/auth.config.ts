// https://content-oyster-10.clerk.accounts.dev

const authConfig = {
  providers: [
    {
      domain: "https://content-oyster-10.clerk.accounts.dev",
      applicationID: "convex",
    },
  ],
};

export default authConfig;
